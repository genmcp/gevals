kind: Eval
metadata:
  name: "gemini-netedge-selector-mismatch"
config:
  agentFile: agent.yaml
  mcpConfigFile: ../mcp-config.yaml
  taskSets:
    - path: ../tasks/selector-mismatch/selector-mismatch.yaml
      assertions:
        toolsUsed:
          - server: netedge
        minToolCalls: 1
        maxToolCalls: 20
