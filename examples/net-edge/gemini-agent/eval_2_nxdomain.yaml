kind: Eval
metadata:
  name: "gemini-netedge-nxdomain-host"
config:
  agentFile: agent.yaml
  mcpConfigFile: ../mcp-config.yaml
  taskSets:
    - path: ../tasks/nxdomain-host/nxdomain-host.yaml
      assertions:
        toolsUsed:
          - server: netedge
            tool: probe_dns_local
        minToolCalls: 1
        maxToolCalls: 20
