kind: Eval
metadata:
  name: "gemini-netedge-referencegrant-missing"
config:
  agent:
    type: file
    path: agent.yaml
  mcpConfigFile: ../mcp-config.yaml
  taskSets:
    - path: ../tasks/referencegrant-missing/referencegrant-missing.yaml
      assertions:
        toolsUsed:
          - server: netedge
        minToolCalls: 1
        maxToolCalls: 20
