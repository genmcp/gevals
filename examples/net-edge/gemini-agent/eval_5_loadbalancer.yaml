kind: Eval
metadata:
  name: "gemini-netedge-loadbalancer-missing"
config:
  agent:
    type: file
    path: agent.yaml
  mcpConfigFile: ../mcp-config.yaml
  taskSets:
    - path: ../tasks/loadbalancer-missing/loadbalancer-missing.yaml
      assertions:
        toolsUsed:
          - server: netedge
        minToolCalls: 1
        maxToolCalls: 20
