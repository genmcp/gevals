kind: Eval
metadata:
  name: "netedge-loadbalancer-missing"
config:
  agentFile: agent.yaml
  mcpConfigFile: ../mcp-config.yaml
  taskSets:
    - path: ../tasks/loadbalancer-missing/loadbalancer-missing.yaml
      assertions:
        toolsUsed:
          - server: netedge
        minToolCalls: 1
        maxToolCalls: 20
