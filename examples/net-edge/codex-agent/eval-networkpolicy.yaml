kind: Eval
metadata:
  name: "netedge-networkpolicy-block"
config:
  agentFile: agent.yaml
  mcpConfigFile: ../mcp-config.yaml
  taskSets:
    - path: ../tasks/networkpolicy-block/networkpolicy-block.yaml
      assertions:
        toolsUsed:
          - server: netedge
            tool: inspect_route
        minToolCalls: 1
        maxToolCalls: 20
